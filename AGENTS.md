# RT Agent Notes

If the task is to install or re-initialize this repository in a Replit workspace, follow the instructions in `docs/INSTALL.md`.
- Conduct the entire installation process in Chinese.
- If the task is to confirm which Replit AI Integrations models are currently usable, use `docs/MODEL_PROBING_PROMPT.md` as the source prompt instead of relying only on guesses or old model lists.
